- if current_user
  %div{ 'ng-controller' => 'ApplicationController',
    'ng-init' => "initialize({user: #{current_user.to_json}, slack: #{@slack_integration.to_json}})" }
    = render 'header'
    = render 'dashboard'
    = render 'settings'
- else
  = render 'landing'
